---
const { imageUrl } = Astro.props;

---

<div
  class="parallax-section h-screen flex items-center justify-center text-white"
  style={`background-image: url('${imageUrl}')`}
>
  <div class="bg-black/50 px-6 py-4 rounded-lg text-center text-3xl font-semibold">
    {<slot />}
  </div>
</div>
